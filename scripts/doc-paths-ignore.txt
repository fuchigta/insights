# check-doc-paths.sh が「実在しなくてよい」とみなすパス。
#
# ドキュメントは、まだ無いものを例として挙げることがある。そういう参照まで
# 実在を要求すると、書けることが減ってドキュメントのほうが痩せる。
# 1 行に 1 パスを書き、なぜ実在しないのかを直前のコメントに残すこと。

# 「Codex に対応するならこう置く」という将来の拡張点の例（docs/development.md）。
internal/source/codex
