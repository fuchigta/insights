# ビルド成果物
/insights
/insights.exe
*.exe
*.test
*.out

# ローカルのデータベース・レポート（利用者ごとの生成物）
*.db
*.db-wal
*.db-shm
/reports/

# エディタ・OS
.DS_Store
Thumbs.db
.idea/
.vscode/

# Claude Code のセッション状態（プラン等）。共有する必要のある設定を
# 置く場合はこのルールを外すこと。
/.claude/

# 開発中のレビューメモ。公開リポジトリに載せる性質のものではないため除外している。
# 公開して問題ない内容であれば、この行を消してコミットすればよい。
/REVIEW.md
